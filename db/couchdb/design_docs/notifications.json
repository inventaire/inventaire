{
   "_id": "_design/notifications",
   "language": "coffeescript",
   "views": {
       "byUserAndTime": {
           "map": "(doc)->\n  emit [doc.user, doc.time], null"
       },
       "bySubject": {
           "map": "(doc)->\n  if doc.type is 'groupUpdate'\n    emit doc.data.group, 'group'\n    emit doc.data.user\t, 'user'"
       }
   }
}