{{>paragraph_top}}
  {{#if user}}
    <h4>{{user.username}}</h4>
    {{user.email}} - {{user._id}}
    confirmed email: {{user.validEmail}}
  {{else}}
    <h4>{{unknownUser}}</h4>
  {{/if}}
  <hr>
  <p>
    {{subject}}
  </p>
  <hr>
  <p>
    {{message}}
  </p>
{{>paragraph_bottom}}
