{
   "_id": "_design/patches",
   "language": "coffeescript",
   "views": {
       "byEntity": {
           "map": "(doc)->\n  if doc.type is 'patch'\n    emit doc._id.split(':'), null"
       }
   }
}