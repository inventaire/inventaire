{
   "_id": "_design/users",
   "language": "coffeescript",
   "views": {
       "byEmail": {
           "map": "(doc)->\n  emit doc.email, doc"
       },
       "byUsername": {
           "map": "(doc)->\n  emit(doc.username.toLowerCase(), doc)"
       }
   }
}
